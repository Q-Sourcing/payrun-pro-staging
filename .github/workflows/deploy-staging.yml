# ==========================================================
# 🧩 Q-Payroll GitHub Actions Workflow for Staging
# ==========================================================
# Note: Lovable auto-deploys based on branch pushes

name: Build Staging
on:
  push:
    branches:
      - staging

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Install dependencies
        run: npm install

      - name: Build application
        run: npm run build

      - name: Success
        run: |
          echo "✅ Build completed successfully for STAGING environment"
          echo "🚀 Lovable will auto-deploy from staging branch"
